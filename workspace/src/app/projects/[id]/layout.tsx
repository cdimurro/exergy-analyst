"use client";

/**
 * Project workspace layout — the workspace is full-screen,
 * so it renders children directly.
 */
export default function ProjectLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
